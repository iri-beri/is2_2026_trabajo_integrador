<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Registro de Estudiante</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen p-4">
    <div class="bg-white p-8 rounded-lg shadow-lg w-full max-w-md">
        <h1 class="text-3xl font-bold text-center text-gray-800 mb-6">Registrar Nuevo Estudiante
        </h1>

        {{#successMessage}}
        <!-- Estilo de mensaje de éxito idéntico al user_form -->
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{successMessage}}</span>
        </div>
        {{/successMessage}}

        {{#errorMessage}}
        <!-- Estilo de mensaje de error idéntico al user_form -->
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
            <span class="block sm:inline">{{errorMessage}}</span>
        </div>
        {{/errorMessage}}

        <form action="/student/new" method="post" class="space-y-4">
            <!-- SECCIÓN OBLIGATORIA: DATOS DE PERSONA -->
            <h2 class="text-xl font-semibold text-gray-700 mb-2">Información Obligatoria
                <span class="text-red-500 text-sm">(*)</span>
            </h2>

            <!-- Nombre -->
            <div>
                <label for="name" 
                        class="block text-gray-700 text-sm font-medium mb-1">Nombre (*):
                </label>
                
                <input type="text"
                        id="name"
                        name="name"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Apellido -->
            <div>
                <label for="surname"
                        class="block text-gray-700 text-sm font-medium mb-1">Apellido (*):
                </label>

                <input type="text"
                        id="surname"
                        name="surname"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- DNI -->
            <div>
                <label for="dni"
                        class="block text-gray-700 text-sm font-medium mb-1">
                        DNI
                </label>

                <input type="text"
                        id="dni"
                        name="dni"
                        required
                        pattern="^[0-9]{7,8}$"
                        title="El DNI debe tener 7 u 8 dígitos"
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Email -->
            <div>
                <label for="email"
                        class="block text-gray-700 text-sm font-medium mb-1">
                        Email
                </label>

                <input type="email"
                        id="email"
                        name="email"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Username -->
            <div>
                <label for="username"
                        class="block text-gray-700 text-sm font-medium mb-1">
                        Nombre de usuario
                </label>

                <input type="text"
                        id="username"
                        name="username"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Password -->
            <div>
                <label for="password"
                        class="block text-gray-700 text-sm font-medium mb-1">
                        Contraseña
                </label>

                <input type="password"
                        id="password"
                        name="password"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- SECCIÓN OPCIONAL: DATOS DE ESTUDIANTE -->
            <h2 class="text-xl font-semibold text-gray-700 mb-2 pt-4 border-t border-gray-200">Información Opcional
            </h2>

            <!-- Fecha nacimiento -->
            <div>
                <label for="birthdate"
                        class="block text-gray-700 text-sm font-medium mb-1">
                        Fecha de nacimiento
                </label>

                <input type="date"
                        id="birthdate"
                        name="birthdate"
                        min="1900-01-01"
                        required
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Lugar de nacimiento -->
            <div>
                <label for="birthplace"
                        class="block text-gray-700 text-sm font-medium mb-1">Lugar de nacimiento:
                </label>
                
                <input type="text" id="birthplace" name="birthplace"
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Lugar de residencia -->
            <div>
                <label for="town_of_residence"
                        class="block text-gray-700 text-sm font-medium mb-1">Lugar de residencia:
                </label>

                <input type="text" id="town_of_residence" name="town_of_residence"
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Contacto familiar -->
            <div>
                <label for="contact_relative"
                            class="block text-gray-700 text-sm font-medium mb-1">Contacto familiar:
                </label>

                <input type="text" id="contact_relative" name="contact_relative"
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <!-- Celular del contacto -->
            <div>
                <label for="cellphone"
                        class="block text-gray-700 text-sm font-medium mb-1">
                    Celular del contacto
                </label>

                <input type="text"
                        id="cellphone"
                        name="cellphone"
                        pattern="^[0-9]{8,16}$"
                        title="Ingrese sólo números (8 a 16 dígitos)"
                        class="w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <button type="submit"
                    class="w-full bg-indigo-600 text-white py-2 px-4 rounded-md hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 mt-6">
                Registrar Estudiante
            </button>

            <div class="text-center mt-4">
                <!-- El enlace se corrigió para ir al dashboard -->
                <a href="/dashboard" class="text-blue-600 hover:underline text-sm">Cancelar</a>
            </div>
        </form>
    </div>
</body>
</html>
